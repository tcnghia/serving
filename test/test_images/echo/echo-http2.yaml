apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: echo-h2
  namespace: default
spec:
  template:
    spec:
      containers:
      - image: ko://knative.dev/serving/test/test_images/echo
        args:
        - --http2=true
